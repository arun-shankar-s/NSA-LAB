touce filenmae--to create

"When a user enters a number like 123 in Bash
, it's stored as a string '123'.
To perform arithmetic operations on such input,
you'd use constructs like $(( expression )) 
to evaluate and convert the string input into a numeric value for computation."

Concatenation
A+=$B
${A}${B}

copy=cp "f1" "f2"
rename= mv "f1" "f2"
remove=rm "f1"
